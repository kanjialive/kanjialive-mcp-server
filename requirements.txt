# Core MCP dependencies
# Required for FastMCP server implementation
mcp>=1.0.0

# HTTP client for API requests
# Used for async requests to Kanji Alive API with retry logic
# Requires async support (AsyncClient)
httpx>=0.27.0

# Data validation and settings management
# Used for input validation, field validators, and Pydantic models
# Requires v2.x for field_validator decorator and ConfigDict
pydantic>=2.0.0

# Note: Tested with the following versions:
# - mcp==1.18.0
# - httpx==0.28.1
# - pydantic==2.12.3
